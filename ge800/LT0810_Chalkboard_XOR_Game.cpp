
#include "../header/myheader.h"

class LT0810
{
public:


// XOR of all elements, 是什么操作， 1，1，1，0，  先去重成 1，0，然后XOR， 还是 1 xor 1 xor 1 xor 0 ? 不过结果好像是一样的。。不一样，1,1,1, 应该是后者。
// 保证 每个num的个数是奇数？ 除了0。。还有 101，1，100， 这种组合。
//
    bool lt0810a(vector<int>& nums)
    {
        return false;
    }

};

int main()
{

    LT0810 lt;


    return 0;
}
