
#include "../header/myheader.h"

class LT0835
{
public:


// discuss
// 取得A/B中 值为1的所有点 下标，A 和 B的 值为1的点遍历，计算X，Y轴的位移，组成string，map[string]++, 值最大的就是结果。

// 坐标最大30，所以可以 x*100+y 来表示 位移。

// 用x*100+y来表示 1的位置。 然后 相减的值是位移。

    int lt0835a(vector<vector<int>>& A, vector<vector<int>>& B)
    {
        return -1;
    }

};

int main()
{

    LT0835 lt;


    return 0;
}
